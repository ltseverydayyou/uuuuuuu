local __lt = (function()
	local globalEnv = (getgenv and getgenv()) or _G or {};
	local sharedEnv = rawget(_G, "shared");
	local cacheHost = type(sharedEnv) == "table" and sharedEnv or (type(globalEnv) == "table" and globalEnv or nil);
	if cacheHost then
		local cached = rawget(cacheHost, "__lt_service_resolver");
		if type(cached) == "table" then
			return cached;
		end;
	end;
	local loader = loadstring or load;
	if type(loader) ~= "function" then
		error("Service resolver loader unavailable");
	end;
	local resolver = loader(game:HttpGet("https://ltseverydayyou.github.io/ServiceResolver.luau"), "@ServiceResolver.luau");
	if type(resolver) ~= "function" then
		error("Service resolver failed to compile");
	end;
	local loaded = resolver();
	if type(loaded) ~= "table" then
		error("Service resolver failed to load");
	end;
	if cacheHost then
		cacheHost.__lt_service_resolver = loaded;
	end;
	return loaded;
end)();

(getgenv()).swordFighterActive = false;
(getgenv()).targetMode = "Player";
local function SafeGetService(n)
	local S = function(_, serviceName) return __lt.gs(serviceName); end;
	local R = cloneref or function(x)
		return x;
	end;
	return __lt.cs(n, R);
end;
local Players = SafeGetService("Players");
local LocalPlayer = Players.LocalPlayer;
local RunService = SafeGetService("RunService");
local UserInput = SafeGetService("UserInputService");
local Workspace = workspace;
local IsOnMobile = (function()
	local platform = (SafeGetService("UserInputService")):GetPlatform();
	if platform == Enum.Platform.IOS or platform == Enum.Platform.Android or platform == Enum.Platform.AndroidTV or platform == Enum.Platform.Chromecast or platform == Enum.Platform.MetaOS then
		return true;
	end;
	if platform == Enum.Platform.None then
		return (SafeGetService("UserInputService")).TouchEnabled and (not ((SafeGetService("UserInputService")).KeyboardEnabled or (SafeGetService("UserInputService")).MouseEnabled));
	end;
	return false;
end)();
local function getCharacter(x)
	if x and x:IsA("Player") then
		return x.Character;
	end;
	return x;
end;
local function getHumanoid(c)
	if not c then
		return;
	end;
	if c:IsA("Player") then
		c = c.Character;
	end;
	return c and c:IsA("Model") and c:FindFirstChildOfClass("Humanoid");
end;
local function getRootPart(c)
	if not c then
		return;
	end;
	if c:IsA("Player") then
		c = c.Character;
	end;
	if c and c:IsA("Model") then
		local p = c:FindFirstChild("HumanoidRootPart") or c:FindFirstChild("Torso") or c:FindFirstChild("UpperTorso") or c:FindFirstChild("LowerTorso");
		if p and p:IsA("BasePart") then
			return p;
		end;
		for _, v in pairs(c:GetChildren()) do
			if v:IsA("BasePart") then
				return v;
			end;
		end;
	end;
end;
local npcList = {};
task.spawn(function()
	while true do
		local newList = {};
		for _, inst in ipairs(Workspace:QueryDescendants("Instance")) do
			if inst:IsA("Model") then
				local h = inst:FindFirstChildOfClass("Humanoid");
				if h and h.Health > 0 and (not __lt.cm("Players", "GetPlayerFromCharacter", inst)) and (not inst:FindFirstChildOfClass("ForceField")) then
					newList[(#newList) + 1] = inst;
				end;
			end;
		end;
		npcList = newList;
		task.wait(0.5);
	end;
end);
local function findClosestPlayer()
	local me = getRootPart(LocalPlayer);
	if not me then
		return;
	end;
	local best, bd = nil, math.huge;
	for _, p in ipairs(__lt.cm("Players", "GetPlayers")) do
		if p ~= LocalPlayer then
			local c = getCharacter(p);
			if not c:FindFirstChildOfClass("ForceField") then
				local r = getRootPart(c);
				local h = getHumanoid(p);
				if r and h and h.Health > 0 then
					local d = (r.Position - me.Position).Magnitude;
					if d < bd then
						best, bd = p, d;
					end;
				end;
			end;
		end;
	end;
	return best;
end;
local function findClosestNPC()
	local me = getRootPart(LocalPlayer);
	if not me then
		return;
	end;
	local best, bd = nil, math.huge;
	for _, m in ipairs(npcList) do
		local r = getRootPart(m);
		local h = getHumanoid(m);
		if r and h and h.Health > 0 then
			local d = (r.Position - me.Position).Magnitude;
			if d < bd then
				best, bd = m, d;
			end;
		end;
	end;
	return best;
end;
local TURN_SPEED = math.rad(280);
local function wrapAngle(angle)
	return math.atan2(math.sin(angle), math.cos(angle));
end;
local function getYawFromLookVector(lookVector)
	return math.atan2(-lookVector.X, -lookVector.Z);
end;
local function faceTargetSmooth(root, targetPosition, deltaTime)
	local planarOffset = Vector3.new(targetPosition.X - root.Position.X, 0, targetPosition.Z - root.Position.Z);
	if planarOffset.Magnitude <= 0.001 then
		return;
	end;
	local currentYaw = getYawFromLookVector(root.CFrame.LookVector);
	local desiredYaw = getYawFromLookVector(planarOffset.Unit);
	local yawDelta = wrapAngle(desiredYaw - currentYaw);
	local maxStep = TURN_SPEED * math.max(deltaTime or 0, 0);
	if math.abs(yawDelta) <= maxStep then
		currentYaw = desiredYaw;
	else
		currentYaw += math.sign(yawDelta) * maxStep;
	end;
	root.CFrame = CFrame.new(root.Position) * CFrame.Angles(0, currentYaw, 0);
end;
local MOVEMENT_RANDOM = Random.new();
local MOVE_UPDATE_RANGE = NumberRange.new(0.06, 0.12);
local STRAFE_INTERVAL_RANGE = NumberRange.new(0.45, 1.05);
local DODGE_INTERVAL_RANGE = NumberRange.new(1.1, 2.1);
local DODGE_DURATION_RANGE = NumberRange.new(0.18, 0.35);
local JUMP_INTERVAL_RANGE = NumberRange.new(0.85, 1.9);
local EDGE_CHECK_HEIGHT = 10;
local EDGE_CHECK_DEPTH = 40;
local EDGE_SIDE_OFFSET = 1.8;
local PATH_SAMPLE_SPACING = 1.5;
local MAX_SAFE_DROP = 4.5;
local MAX_STEP_UP = 3.75;
local FLOOR_TARGET_LIFT = 2;
local VOID_LOOKAHEAD_DISTANCE = 4;
local JUMP_PATH_SAMPLE_SPACING = 0.75;
local MIN_JUMP_GAP = 1.25;
local MAX_JUMP_GAP = 7.5;
local JUMP_LANDING_DISTANCE = 9;
local JUMP_COMMIT_TIME = 0.9;
local JUMP_GOAL_REACHED_DISTANCE = 3.5;
local movementState = {
	targetHum = nil,
	nextMoveUpdate = 0,
	nextStrafeFlip = 0,
	nextDodgeAt = 0,
	dodgeUntil = 0,
	nextJumpAt = 0,
	activeJumpGoal = nil,
	jumpCommitUntil = 0,
	strafeOffset = 0,
	dodgeOffset = 0
};
local function nextRandom(range)
	return MOVEMENT_RANDOM:NextNumber(range.Min, range.Max);
end;
local function getFlatVector(vector, fallback)
	local flat = Vector3.new(vector.X, 0, vector.Z);
	if flat.Magnitude <= 0.001 then
		return fallback;
	end;
	return flat.Unit;
end;
local function buildGroundParams(ignoreList)
	local params = RaycastParams.new();
	params.FilterType = Enum.RaycastFilterType.Exclude;
	params.FilterDescendantsInstances = ignoreList;
	params.IgnoreWater = false;
	return params;
end;
local function getFloorHit(position, ignoreList)
	local origin = Vector3.new(position.X, position.Y + EDGE_CHECK_HEIGHT, position.Z);
	return Workspace:Raycast(origin, Vector3.new(0, -EDGE_CHECK_DEPTH, 0), buildGroundParams(ignoreList));
end;
local function isFloorSafe(hit, referenceY)
	if not hit or hit.Normal.Y < 0.5 then
		return false;
	end;
	local floorDelta = hit.Position.Y - referenceY;
	return floorDelta <= MAX_STEP_UP and floorDelta >= -MAX_SAFE_DROP;
end;
local function getFloorPoint(position, ignoreList, referenceY)
	local hit = getFloorHit(position, ignoreList);
	if not isFloorSafe(hit, referenceY) then
		return nil;
	end;
	return Vector3.new(position.X, hit.Position.Y + FLOOR_TARGET_LIFT, position.Z), hit.Position.Y;
end;
local function isStandingZoneSafe(position, forward, ignoreList, referenceY)
	local centerPoint, centerFloorY = getFloorPoint(position, ignoreList, referenceY);
	if not centerPoint then
		return false;
	end;
	local right = Vector3.new(-forward.Z, 0, forward.X);
	local leftPoint = getFloorPoint(centerPoint + right * EDGE_SIDE_OFFSET, ignoreList, centerFloorY);
	local rightPoint = getFloorPoint(centerPoint - right * EDGE_SIDE_OFFSET, ignoreList, centerFloorY);
	return leftPoint ~= nil and rightPoint ~= nil;
end;
local function isPathSafe(origin, destination, forward, ignoreList)
	local planar = Vector3.new(destination.X - origin.X, 0, destination.Z - origin.Z);
	local distance = planar.Magnitude;
	local _, floorY = getFloorPoint(origin, ignoreList, origin.Y);
	if not floorY then
		return false;
	end;
	if distance <= 0.001 then
		return isStandingZoneSafe(destination, forward, ignoreList, floorY);
	end;
	local direction = planar.Unit;
	local steps = math.max(1, math.ceil(distance / PATH_SAMPLE_SPACING));
	for i = 1, steps do
		local alpha = i / steps;
		local sample = origin + planar * alpha;
		local _, sampleFloorY = getFloorPoint(sample, ignoreList, floorY);
		if not sampleFloorY then
			return false;
		end;
		floorY = sampleFloorY;
	end;
	return isStandingZoneSafe(destination, direction, ignoreList, floorY);
end;
local function getJumpLandingPoint(origin, forward, ignoreList)
	local _, originFloorY = getFloorPoint(origin, ignoreList, origin.Y);
	if not originFloorY then
		return nil;
	end;
	local gapStartedAt = nil;
	local distance = JUMP_PATH_SAMPLE_SPACING;
	while distance <= JUMP_LANDING_DISTANCE do
		local sample = origin + forward * distance;
		local landingPoint, landingFloorY = getFloorPoint(sample, ignoreList, originFloorY);
		if landingPoint then
			if gapStartedAt and (distance - gapStartedAt) >= MIN_JUMP_GAP and (distance - gapStartedAt) <= MAX_JUMP_GAP then
				if isStandingZoneSafe(landingPoint, forward, ignoreList, landingFloorY) then
					return landingPoint;
				end;
			end;
		elseif not gapStartedAt then
			gapStartedAt = distance;
		end;
		distance += JUMP_PATH_SAMPLE_SPACING;
	end;
	return nil;
end;
local function getJumpMoveGoal(origin, forward, right, ignoreList)
	local offsets = { 0, 1.5, -1.5, 2.5, -2.5 };
	for _, lateralOffset in ipairs(offsets) do
		local shiftedOrigin = origin + right * lateralOffset;
		local landingPoint = getJumpLandingPoint(shiftedOrigin, forward, ignoreList);
		if landingPoint then
			return Vector3.new(landingPoint.X, landingPoint.Y, landingPoint.Z);
		end;
	end;
	return nil;
end;
local function resetMovementState(targetHum)
	movementState.targetHum = targetHum;
	movementState.nextMoveUpdate = 0;
	movementState.nextStrafeFlip = 0;
	movementState.nextDodgeAt = 0;
	movementState.dodgeUntil = 0;
	movementState.nextJumpAt = 0;
	movementState.activeJumpGoal = nil;
	movementState.jumpCommitUntil = 0;
	movementState.strafeOffset = 0;
	movementState.dodgeOffset = 0;
end;
local function setActiveJumpGoal(goal, now)
	movementState.activeJumpGoal = goal;
	movementState.jumpCommitUntil = now + JUMP_COMMIT_TIME;
end;
local function clearActiveJumpGoal()
	movementState.activeJumpGoal = nil;
	movementState.jumpCommitUntil = 0;
end;
local function isAirborneState(state)
	return state == Enum.HumanoidStateType.Jumping or state == Enum.HumanoidStateType.Freefall;
end;
local function shouldMaintainJumpGoal(root, hum, now)
	local jumpGoal = movementState.activeJumpGoal;
	if not jumpGoal then
		return false;
	end;
	if (Vector3.new(root.Position.X, 0, root.Position.Z) - Vector3.new(jumpGoal.X, 0, jumpGoal.Z)).Magnitude <= JUMP_GOAL_REACHED_DISTANCE then
		clearActiveJumpGoal();
		return false;
	end;
	local state = hum:GetState();
	if isAirborneState(state) then
		return true;
	end;
	if now <= movementState.jumpCommitUntil then
		return true;
	end;
	clearActiveJumpGoal();
	return false;
end;
local function updateMovementState(now, distanceToTarget)
	if now >= movementState.nextStrafeFlip then
		local strafeSign = MOVEMENT_RANDOM:NextInteger(0, 1) == 0 and -1 or 1;
		movementState.strafeOffset = strafeSign * MOVEMENT_RANDOM:NextNumber(1.1, 2.6);
		movementState.nextStrafeFlip = now + nextRandom(STRAFE_INTERVAL_RANGE);
	end;
	if now >= movementState.dodgeUntil then
		movementState.dodgeOffset = 0;
		if distanceToTarget <= 18 and now >= movementState.nextDodgeAt then
			local dodgeSign = MOVEMENT_RANDOM:NextInteger(0, 1) == 0 and -1 or 1;
			movementState.dodgeOffset = dodgeSign * MOVEMENT_RANDOM:NextNumber(3.2, 5);
			movementState.dodgeUntil = now + nextRandom(DODGE_DURATION_RANGE);
			movementState.nextDodgeAt = now + nextRandom(DODGE_INTERVAL_RANGE);
		end;
	end;
end;
local function getRecoveryGoal(root, targetRoot, ignoreList)
	local rootPosition = Vector3.new(root.Position.X, root.Position.Y, root.Position.Z);
	local retreatForward = getFlatVector(rootPosition - targetRoot.Position, getFlatVector(-root.CFrame.LookVector, Vector3.new(0, 0, 1)));
	local retreatRight = Vector3.new(-retreatForward.Z, 0, retreatForward.X);
	local candidates = {
		rootPosition + retreatForward * 4,
		rootPosition + retreatRight * 4,
		rootPosition - retreatRight * 4,
		rootPosition + retreatForward * 2
	};
	for _, candidate in ipairs(candidates) do
		if isPathSafe(rootPosition, candidate, retreatForward, ignoreList) then
			local floorPoint = getFloorPoint(candidate, ignoreList, root.Position.Y);
			return floorPoint or candidate;
		end;
	end;
	return rootPosition;
end;
local function getMovementGoal(root, targetRoot, ignoreList, now)
	local rootPosition = Vector3.new(root.Position.X, root.Position.Y, root.Position.Z);
	local targetPosition = Vector3.new(targetRoot.Position.X, root.Position.Y, targetRoot.Position.Z);
	local forward = getFlatVector(targetPosition - rootPosition, getFlatVector(root.CFrame.LookVector, Vector3.new(0, 0, -1)));
	local right = Vector3.new(-forward.Z, 0, forward.X);
	local planarDistance = (targetPosition - rootPosition).Magnitude;
	updateMovementState(now, planarDistance);
	local preferredDistance = math.clamp(planarDistance * 0.35, 3, 6);
	local preferred = targetPosition - forward * preferredDistance;
	preferred += right * (movementState.strafeOffset + movementState.dodgeOffset);
	preferred += forward * MOVEMENT_RANDOM:NextNumber(-0.6, 0.7);
	local candidates = {
		preferred,
		targetPosition - forward * preferredDistance + right * movementState.strafeOffset,
		targetPosition - forward * 3.5,
		rootPosition + forward * math.clamp(planarDistance * 0.45, 2, 5),
		rootPosition - forward * 3,
		rootPosition + right * 3,
		rootPosition - right * 3,
		rootPosition
	};
	for _, candidate in ipairs(candidates) do
		local moveTarget = Vector3.new(candidate.X, root.Position.Y, candidate.Z);
		if isPathSafe(rootPosition, moveTarget, forward, ignoreList) then
			local floorPoint = getFloorPoint(moveTarget, ignoreList, root.Position.Y);
			return floorPoint or moveTarget, false;
		end;
	end;
	local jumpGoal = getJumpMoveGoal(rootPosition, forward, right, ignoreList);
	if jumpGoal then
		return jumpGoal, true;
	end;
	return getRecoveryGoal(root, targetRoot, ignoreList), false;
end;
local function shouldJump(hum, root, targetRoot, ignoreList, now)
	if now < movementState.nextJumpAt then
		return false;
	end;
	movementState.nextJumpAt = now + nextRandom(JUMP_INTERVAL_RANGE);
	if hum.FloorMaterial == Enum.Material.Air then
		return false;
	end;
	local state = hum:GetState();
	if state == Enum.HumanoidStateType.Jumping or state == Enum.HumanoidStateType.Freefall then
		return false;
	end;
	local distance = (Vector3.new(root.Position.X, 0, root.Position.Z) - Vector3.new(targetRoot.Position.X, 0, targetRoot.Position.Z)).Magnitude;
	if distance > 14 then
		return false;
	end;
	local forward = getFlatVector(targetRoot.Position - root.Position, getFlatVector(root.CFrame.LookVector, Vector3.new(0, 0, -1)));
	local right = Vector3.new(-forward.Z, 0, forward.X);
	if movementState.activeJumpGoal or getJumpMoveGoal(root.Position, forward, right, ignoreList) then
		return true;
	end;
	local jumpCheck = root.Position + forward * 2;
	return isPathSafe(root.Position, jumpCheck, forward, ignoreList) and MOVEMENT_RANDOM:NextNumber(0, 1) <= 0.65;
end;
local function hasVoidAhead(root, targetRoot, ignoreList)
	local rootPosition = Vector3.new(root.Position.X, root.Position.Y, root.Position.Z);
	local forward = getFlatVector(targetRoot.Position - root.Position, getFlatVector(root.CFrame.LookVector, Vector3.new(0, 0, -1)));
	local right = Vector3.new(-forward.Z, 0, forward.X);
	local checkPoint = rootPosition + forward * VOID_LOOKAHEAD_DISTANCE;
	if getJumpMoveGoal(rootPosition, forward, right, ignoreList) then
		return false;
	end;
	return not isPathSafe(rootPosition, checkPoint, forward, ignoreList);
end;
local function protectUI(s)
	if s:IsA("ScreenGui") then
		s.ZIndexBehavior = Enum.ZIndexBehavior.Global;
		s.DisplayOrder = 999999999;
		s.ResetOnSpawn = false;
		s.IgnoreGuiInset = true;
	end;
	local core = SafeGetService("CoreGui");
	local lp = Players.LocalPlayer;
	local function NA(i, v)
		if i then
			if v then
				i[v] = "\000";
				i.Archivable = false;
			else
				i.Name = "\000";
				i.Archivable = false;
			end;
		end;
	end;
	if gethui then
		NA(s);
		s.Parent = gethui();
	elseif core and __lt.cm("CoreGui", "FindFirstChild", "RobloxGui") then
		NA(s);
		s.Parent = core.RobloxGui;
	elseif core then
		NA(s);
		s.Parent = core;
	elseif lp and lp:FindFirstChildWhichIsA("PlayerGui") then
		NA(s);
		s.Parent = lp:FindFirstChildWhichIsA("PlayerGui");
		s.ResetOnSpawn = false;
	end;
	return s;
end;
local function NAdragV2(ui, dragui)
	dragui = dragui or ui;
	local UserInputService = SafeGetService("UserInputService");
	local dragging = false;
	local dragInput;
	local dragStart;
	local startPos;
	local function update(input)
		local success, err = pcall(function()
			local delta = input.Position - dragStart;
			local screenSize = ui.Parent.AbsoluteSize;
			local newXScale = startPos.X.Scale + (startPos.X.Offset + delta.X) / screenSize.X;
			local newYScale = startPos.Y.Scale + (startPos.Y.Offset + delta.Y) / screenSize.Y;
			ui.Position = UDim2.new(newXScale, 0, newYScale, 0);
		end);
		if not success then
			warn("[Dragger] update error:", err);
		end;
	end;
	pcall(function()
		dragui.InputBegan:Connect(function(input)
			local success, err = pcall(function()
				if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
					dragging = true;
					dragStart = input.Position;
					startPos = ui.Position;
					pcall(function()
						input.Changed:Connect(function()
							local ok, innerErr = pcall(function()
								if input.UserInputState == Enum.UserInputState.End then
									dragging = false;
								end;
							end);
							if not ok then
								warn("[Dragger] input.Changed error:", innerErr);
							end;
						end);
					end);
				end;
			end);
			if not success then
				warn("[Dragger] InputBegan error:", err);
			end;
		end);
	end);
	pcall(function()
		dragui.InputChanged:Connect(function(input)
			local success, err = pcall(function()
				if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
					dragInput = input;
				end;
			end);
			if not success then
				warn("[Dragger] InputChanged error:", err);
			end;
		end);
	end);
	pcall(function()
		UserInputService.InputChanged:Connect(function(input)
			local success, err = pcall(function()
				if input == dragInput and dragging then
					update(input);
				end;
			end);
			if not success then
				warn("[Dragger] UserInputService.InputChanged error:", err);
			end;
		end);
	end);
	local success, err = pcall(function()
		ui.Active = true;
	end);
	if not success then
		warn("[Dragger] Set Active error:", err);
	end;
end;
local function makeButton(p)
	local b = Instance.new("TextButton");
	b.Size = p.Size;
	b.Position = p.Position;
	b.AnchorPoint = p.AnchorPoint or Vector2.new(0, 0);
	b.Text = p.Text or "";
	b.Font = p.Font or Enum.Font.GothamBold;
	b.TextScaled = true;
	b.BackgroundColor3 = p.BGColor or Color3.fromRGB(30, 30, 30);
	b.TextColor3 = p.TextColor3 or Color3.new(1, 1, 1);
	b.Parent = p.Parent;
	(Instance.new("UICorner", b)).CornerRadius = UDim.new(0.25, 0);
	return b;
end;
local pcLabel;
local function updatePCLabel()
	if pcLabel then
		pcLabel.Text = "Bot: " .. ((getgenv()).swordFighterActive and "ON" or "OFF") .. " | Mode: " .. (getgenv()).targetMode .. " | X:Toggle C:Mode";
	end;
end;
local currentConn, currentHum;
local function setupUI()
	local gui = protectUI(Instance.new("ScreenGui"));
	if IsOnMobile then
		local t = makeButton({
			Parent = gui,
			Size = UDim2.new(0, 80, 0, 30),
			AnchorPoint = Vector2.new(0.5, 1),
			Position = UDim2.new(0.5, 0, 1, -40),
			Text = "Bot: OFF"
		});
		local m = makeButton({
			Parent = gui,
			Size = UDim2.new(0, 80, 0, 30),
			AnchorPoint = Vector2.new(0.5, 1),
			Position = UDim2.new(0.5, 0, 1, -80),
			Text = "Mode: Player"
		});
		t.MouseButton1Click:Connect(function()
			(getgenv()).swordFighterActive = not (getgenv()).swordFighterActive;
			LocalPlayer.Character.Humanoid.AutoRotate = not (getgenv()).swordFighterActive;
			t.Text = (getgenv()).swordFighterActive and "Bot: ON" or "Bot: OFF";
		end);
		m.MouseButton1Click:Connect(function()
			(getgenv()).targetMode = (getgenv()).targetMode == "Player" and "NPC" or "Player";
			m.Text = "Mode:" .. (getgenv()).targetMode;
			if currentConn then
				currentConn:Disconnect();
			end;
			currentConn, currentHum = nil, nil;
		end);
		NAdragV2(t);
		NAdragV2(m);
	else
		pcLabel = Instance.new("TextLabel");
		pcLabel.Size = UDim2.new(0, 300, 0, 30);
		pcLabel.AnchorPoint = Vector2.new(0.5, 1);
		pcLabel.Position = UDim2.new(0.5, 0, 1, -20);
		pcLabel.Font = Enum.Font.GothamBold;
		pcLabel.TextScaled = true;
		pcLabel.BackgroundColor3 = Color3.fromRGB(30, 30, 30);
		pcLabel.TextColor3 = Color3.new(1, 1, 1);
		pcLabel.Parent = gui;
		(Instance.new("UICorner", pcLabel)).CornerRadius = UDim.new(0.25, 0);
		updatePCLabel();
	end;
end;
setupUI();
UserInput.InputBegan:Connect(function(inp, gp)
	if gp then
		return;
	end;
	if inp.UserInputType == Enum.UserInputType.Keyboard then
		if inp.KeyCode == Enum.KeyCode.X then
			(getgenv()).swordFighterActive = not (getgenv()).swordFighterActive;
			LocalPlayer.Character.Humanoid.AutoRotate = not (getgenv()).swordFighterActive;
			updatePCLabel();
		elseif inp.KeyCode == Enum.KeyCode.C then
			(getgenv()).targetMode = (getgenv()).targetMode == "Player" and "NPC" or "Player";
			updatePCLabel();
			if currentConn then
				currentConn:Disconnect();
			end;
			currentConn, currentHum = nil, nil;
		end;
	end;
end);
RunService.RenderStepped:Connect(function(deltaTime)
	if not (getgenv()).swordFighterActive then
		return;
	end;
	local char = LocalPlayer.Character;
	local hum = getHumanoid(char);
	local root = getRootPart(char);
	if not hum or (not root) then
		return;
	end;
	local tgt;
	if (getgenv()).targetMode == "Player" then
		tgt = findClosestPlayer();
	else
		tgt = findClosestNPC();
	end;
	if not tgt then
		return;
	end;
	local trChar = getCharacter(tgt);
	local trHum = getHumanoid(trChar);
	local trRoot = getRootPart(trChar);
	if not trHum or (not trRoot) or trHum.Health <= 0 then
		return;
	end;
	if movementState.targetHum ~= trHum then
		resetMovementState(trHum);
	end;
	if trHum ~= currentHum then
		if currentConn then
			currentConn:Disconnect();
		end;
		local hookMode = (getgenv()).targetMode;
		currentConn = trHum.StateChanged:Connect(function(_, s)
			if (getgenv()).swordFighterActive and (getgenv()).targetMode == hookMode and (s == Enum.HumanoidStateType.Freefall or s == Enum.HumanoidStateType.Jumping) then
				local mh = getHumanoid(LocalPlayer);
				if mh and mh:GetState() ~= Enum.HumanoidStateType.Freefall and mh:GetState() ~= Enum.HumanoidStateType.Jumping then
					mh:ChangeState(Enum.HumanoidStateType.Jumping);
				end;
			end;
		end);
		currentHum = trHum;
	end;
	faceTargetSmooth(root, trRoot.Position, deltaTime);
	local now = os.clock();
	local ignoreList = { char, trChar };
	if shouldMaintainJumpGoal(root, hum, now) then
		hum:MoveTo(movementState.activeJumpGoal);
		movementState.nextMoveUpdate = now + nextRandom(MOVE_UPDATE_RANGE);
	elseif hasVoidAhead(root, trRoot, ignoreList) then
		clearActiveJumpGoal();
		hum:MoveTo(getRecoveryGoal(root, trRoot, ignoreList));
		movementState.nextMoveUpdate = now + nextRandom(MOVE_UPDATE_RANGE);
	elseif now >= movementState.nextMoveUpdate then
		local moveGoal, isJumpGoal = getMovementGoal(root, trRoot, ignoreList, now);
		if isJumpGoal then
			setActiveJumpGoal(moveGoal, now);
		else
			clearActiveJumpGoal();
		end;
		hum:MoveTo(moveGoal);
		movementState.nextMoveUpdate = now + nextRandom(MOVE_UPDATE_RANGE);
	end;
	if shouldJump(hum, root, trRoot, ignoreList, now) then
		hum:ChangeState(Enum.HumanoidStateType.Jumping);
	end;
	local tool = char:FindFirstChildOfClass("Tool");
	local handle = tool and tool:FindFirstChild("Handle");
	if handle and (root.Position - trRoot.Position).Magnitude <= 8 then
		tool:Activate();
		for _, p in ipairs(trChar:QueryDescendants("Instance")) do
			if p:IsA("BasePart") then
				firetouchinterest(handle, p, 0);
				firetouchinterest(handle, p, 1);
			end;
		end;
	end;
end);
task.spawn(function()
	local c = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait();
	local h = c:WaitForChild("Humanoid");
	h:SetStateEnabled(Enum.HumanoidStateType.FallingDown, false);
	h:SetStateEnabled(Enum.HumanoidStateType.Ragdoll, false);
end);
